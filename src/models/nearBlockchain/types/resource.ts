export type Media = {
    proposal_id: number|null;
    name: string;
    category: string;
    type: ResourceType; 
    tags: number[];
    version: string;
    valid: boolean;
}

export type ResourceType = {
    
}